#!/usr/bin/env python3
"""
Документация использования генератора прокси и защиты от бана
===============================================================
"""

documentation = """
╔════════════════════════════════════════════════════════════╗
║        SNOSER-TG 2.0+ с ПРОКСИ И АНТИ-БАН ЗАЩИТОЙ        ║
╚════════════════════════════════════════════════════════════╝

📋 ОСНОВНЫЕ КОМПОНЕНТЫ:
═══════════════════════════════════════════════════════════

1️⃣ ProxyGenerator
   └─ Генерирует случайные публичные прокси
   └─ Методы:
      • generate_free_proxy_list() - список прокси
      • get_random_proxy() - случайный прокси
      • get_proxy_dict() - преобразование в dict для requests

2️⃣ DataRandomizer
   └─ Рандомизирует данные отправителей для избежания фильтров
   └─ Методы:
      • generate_random_user_agent() - случайный User-Agent
      • randomize_delays() - случайная задержка (0.5-3 сек)
      • randomize_sender_name() - случайное отображаемое имя
      • add_variation_to_text() - добавляет вариации в текст
      • randomize_phone_format() - форматирует номер
      • randomize_timing() - дает случайные интервалы

3️⃣ IPRotator
   └─ Ротирует IP адреса для избежания блокировки
   └─ Методы:
      • get_next_proxy() - следующий в списке
      • get_random_proxy() - случайный
      • add_proxy() - добавить новый
      • remove_bad_proxy() - удалить неработающий

4️⃣ SendingProfile
   └─ Профиль отправителя с уникальными параметрами
   └─ Параметры:
      • email - адрес отправителя
      • display_name - отображаемое имя
      • user_agent - Browser signature
      • proxy - IP адрес
      • send_count - счетчик отправок
      • error_count - счетчик ошибок

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🛡️ ЗАЩИТА ОТ БАНА:
═══════════════════════════════════════════════════════════

Уровни защиты в NewLinks.py:

Level 1: РАНДОМИЗАЦИЯ ЗАДЕРЖЕК
  ├─ Случайная задержка между письмами: 0.5-3 сек
  ├─ Большие паузы каждые 3 отправителя: 5-15 сек
  └─ Вариация в текстах (похожие символы)

Level 2: IP РОТАЦИЯ
  ├─ Смена прокси каждые 5 отправок
  ├─ Ротирование через публичные прокси серверы
  └─ Трекинг количества ротаций

Level 3: USER-AGENT ВАРИАЦИЯ
  ├─ Случайный User-Agent для каждого письма
  ├─ Симуляция разных браузеров
  └─ Разные ОС (Windows, Mac, Linux, Mobile)

Level 4: ОБРАБОТКА ОШИБОК
  ├─ Перехват SMTP ошибок (Authentication, Connection)
  ├─ Лучшие сообщения об ошибках в логи
  ├─ Таймауты для подключения (10 сек)
  └─ Исключения не прерывают цикл отправки

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🚀 КАК ИСПОЛЬЗОВАТЬ:
═══════════════════════════════════════════════════════════

1. Импортируем компоненты:
   ┌─────────────────────────────────────────────┐
   │ from proxy_generator import (               │
   │     ProxyGenerator,                         │
   │     DataRandomizer,                         │
   │     IPRotator,                              │
   │     SendingProfile                          │
   │ )                                           │
   └─────────────────────────────────────────────┘

2. Создаем IP ротатор:
   ┌─────────────────────────────────────────────┐
   │ ip_rotator = IPRotator()                    │
   │ proxy = ip_rotator.get_next_proxy()         │
   └─────────────────────────────────────────────┘

3. Рандомизируем данные:
   ┌─────────────────────────────────────────────┐
   │ delay = DataRandomizer.randomize_delays()   │
   │ ua = DataRandomizer.generate_random_user_agent() │
   │ name = DataRandomizer.randomize_sender_name() │
   └─────────────────────────────────────────────┘

4. Используем в цикле отправки:
   ┌─────────────────────────────────────────────┐
   │ for idx, (email, pwd) in enumerate(senders):│
   │     if idx % 5 == 0:                        │
   │         proxy = ip_rotator.get_next_proxy() │
   │     delay = DataRandomizer.randomize_delays()│
   │     time.sleep(delay)                       │
   │     send_email(...)                         │
   └─────────────────────────────────────────────┘

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📊 ПРОКСИ СПИСОК:
═══════════════════════════════════════════════════════════

Текущие публичные прокси в генераторе:
  1. 10.10.1.10:3128
  2. 10.10.1.11:1080
  3. 198.101.192.20:80
  4. 195.20.114.31:8080
  5. 196.27.106.76:3128
  6. 203.142.68.45:9999
  7. 61.19.145.113:8080
  8. 103.236.48.165:8000
  9. 203.192.199.114:8080
  10. 123.231.248.41:3128

⚠️ Совет: Добавьте свои прокси через:
   ip_rotator.add_proxy('http://your-proxy:port')

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🔍 ОТЛАДКА:
═══════════════════════════════════════════════════════════

Проверить работу прокси генератора:
  $ python3 proxy_generator.py

Посмотреть логи:
  $ tail -f logs/snoser.log | grep "ротация\\|Ошибка\\|отправлено"

Показать IP ротаций:
  $ grep "ротация" logs/snoser.log

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

⚡ ПРОИЗВОДИТЕЛЬНОСТЬ:
═══════════════════════════════════════════════════════════

Оптимальные интервалы:
  • Между письмами: 0.5-3 сек (избегаем спама)
  • Между отправителями: 5-15 сек (страховка)
  • Ротация IP: каждые 5 отправок (не видно паттерна)

Примерная скорость:
  • 100 писем: ~5-10 минут
  • 1000 писем: ~1-2 часа
  • 5000 писем: ~5-10 часов

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

✅ ТЕСТИРОВАНИЕ:
═══════════════════════════════════════════════════════════

Перед большой рассылкой:
  1. Запустите с 1-2 отправителями на тестовый адрес
  2. Проверьте логи на наличие ошибок
  3. Убедитесь, что письма приходят корректно
  4. Затем масштабируйте на полный список

Команды для тестирования:
  $ python3 NewLinks.py  # Запуск с меню
  $ python3 proxy_generator.py  # Тест компонентов

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📝 ПРИМЕРЫ ВЫВОДА:
═══════════════════════════════════════════════════════════

Успешная отправка:
  ✓ Отправлено user0001@ukr.net -> support@telegram.org
  ✓ успешно отправилось на abuse@telegram.org от sender0002@i.ua!

IP ротация:
  🔄 IP ротация: http://195.20.114.31:8080
  ⏸️  Пауза 8.3сек (защита от бана)...

Ошибка:
  ✗ SMTP ошибка user@mail.ru: [Errno -3] Temporary failure in name resolution
  ✗ Ошибка аутентификации sender@ukr.net

Итоги:
  ═══════════════════════════
  📊 ИТОГОВЫЙ ОТЧЕТ
  ═══════════════════════════
  ✓ Всего отправлено: 42 писем
  ✓ IP ротаций: 8
  
  ✓ Сессия завершена. Всего отправлено: 42 писем

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🎯 РЕКОМЕНДАЦИИ:
═══════════════════════════════════════════════════════════

1. Используйте разнообразные прокси
2. Не отправляйте более 100 писем за раз
3. Оставляйте паузы между сессиями (1-2 часа)
4. Меняйте текст жалоб между сеансами
5. Мониторьте логи на предмет блокировок
6. Добавляйте новые прокси если заметны сбои

╔════════════════════════════════════════════════════════════╗
║         Документация по proxy_generator.py                 ║
║          Версия 2.0+ (2026-05-29)                         ║
╚════════════════════════════════════════════════════════════╝
"""

if __name__ == "__main__":
    print(documentation)
